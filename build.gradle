apply plugin: 'java'
apply plugin: 'idea'

//def toolingApiVersion = gradle.gradleVersion
def toolingApiVersion = 1.2

repositories {
    maven {
        url 'http://repo.gradle.org/gradle/libs-releases-local'
    }
    mavenCentral()
}

dependencies {
    compile "org.gradle:gradle-tooling-api:${toolingApiVersion}"
    // need an SLF4J implementation at runtime
    runtime 'org.slf4j:slf4j-simple:1.6.6'
}

// enable Gradle Wrapper support for bootstrapping with specific version
task wrapper(type: Wrapper) {
    gradleVersion = toolingApiVersion
}

// TODO figure out how to bundle up plugin from Gradle